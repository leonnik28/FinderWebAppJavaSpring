CREATE TABLE dotacharacters (
                                id SERIAL PRIMARY KEY,
                                name VARCHAR(255),
                                power INTEGER,
                                dexterity INTEGER,
                                intelligence INTEGER,
                                appointment VARCHAR(255)
);Почитай про IoC, DI, Bean, Spring Core основные моменты.
  Почитай про аннотации, также про архитектуру Spring приложений.
  Почитай как их создавать, про способы конфига.
  Вместо dao у тебя repository, почитай что под капотом(jdbc, jpa, hibernate).
  Соответсвеноо почитай про сам sql, что как происходит.

  Также после того как на гит зальешь, тебе нужно будет на sonar проверить код,
  я пытался не говнокодить, но всеравно, если вопросы будут сначала спроси у нейросети,
  потом если что мне пиши.

  Для бд, postgresql 13 - 16, для субд pgadmin v7 или v8.
  Бд подключать у тебя справа в ide будет написано database, там жмешь на +,
  дальше postgresql, там указываешь Username, Password, db name - соответвенно все из
  pgadmin. Дальше у тебя в resources -> application.properties лежит файл конфигурации
  проекта, соответственно БД тоже. Почитай крч.
git ignore сам напишешь. Исключения не обрабатывал.